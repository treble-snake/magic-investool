{
  "name": "@investool/root",
  "version": "0.11.3",
  "scripts": {
    "bump-version": "yarn version --${TYPE:-patch} && nx run-many --target=bump-version --${TYPE:-patch}"
  },
  "author": "Cap",
  "license": "MIT",
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/electron",
      "**/electron-builder"
    ]
  },
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "nx": "15.8.5"
  }
}
