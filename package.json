{
  "name": "@investool/root",
  "version": "0.10.1",
  "scripts": {
    "install": "cd ./engine && yarn && yarn build && cd ../cli && yarn && yarn build && cd ../web-ui && yarn && yarn build",
    "postinstall": "yarn upgrade @investool/engine",
    "setup": "node ./cli/dist/index.js",
    "start": "cd ./web-ui && yarn start"
  },
  "author": "Cap <treble.snake@gmail.com>",
  "license": "MIT"
}
